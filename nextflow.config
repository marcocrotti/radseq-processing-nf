docker.enabled = true
process.container = 'mcrotti1/radseq'
docker.runOptions='-u $(id -u):$(id -g)'
docker.runOptions='--volume $HOME:$HOME'

process {
    cpus = 4
    container = 'mcrotti1/radseq'
}



